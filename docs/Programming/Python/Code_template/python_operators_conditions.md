# 🧠 Python – ლოგიკური ტიპი და პირობის ოპერატორები

## 🔹 შესავალი
**გადაწყვეტილების მიღება**.  
თუ რაიმე პირობა მართალია — ვაკეთებთ ერთ რაღაცას, ხოლო თუ არა — სხვას.  
ამისათვის Python-ში არსებობს **ლოგიკური ტიპი (Boolean)** და **პირობის ოპერატორები**.

---

## 🔸 Boolean (ლოგიკური) ტიპი

Python-ში ორი ლოგიკური მნიშვნელობა არსებობს:

```python
True   # მართალი
False  # მცდარი
```

> ეს არ არის სტრინგი, ამიტომ არ უნდა ჩასვათ ბრჭყალებში.

მაგალითი:
```python
x = True
y = False

print(x)       # True
print(type(x)) # <class 'bool'>
```

> Boolean ტიპი ხშირად ჩნდება შედარებისას ან პირობებში.

---

## 🔸 შედარების ოპერატორები (Comparison Operators)

| ოპერატორი | მნიშვნელობა | მაგალითი | შედეგი |
|------------|--------------|-----------|----------|
| `==` | ტოლია | `5 == 5` | True |
| `!=` | არ არის ტოლი | `5 != 3` | True |
| `>` | მეტია | `7 > 4` | True |
| `<` | ნაკლებია | `3 < 8` | True |
| `>=` | მეტია ან ტოლია | `5 >= 5` | True |
| `<=` | ნაკლებია ან ტოლია | `4 <= 6` | True |

მაგალითი:
```python
age = 18
print(age >= 18)   # True
print(age < 21)    # True
print(age == 20)   # False
```

---

## 🔸 ლოგიკური ოპერატორები (Logical Operators)

| ოპერატორი | მნიშვნელობა | მაგალითი | შედეგი |
|------------|-------------|-----------|----------|
| `and` | და | `True and True` | True |
| `or` | ან | `True or False` | True |
| `not` | არ | `not True` | False |

მაგალითი:
```python
a = 5
b = 10

print(a > 3 and b < 15)  # True  (ორივე პირობა მართალია)
print(a > 3 or b > 20)   # True  (ერთი მაინც მართალია)
print(not(a == 5))       # False (პირობა მართალია, ამიტომ not გარდაქმნის False-ად)
```

---

## 🔸 პირობის ოპერატორები (Conditional Statements)

პირობისთვის ვიყენებთ `if`, `elif`, `else` სტრუქტურას:

```python
if პირობა:
    # ეს ბლოკი შესრულდება თუ პირობა მართალია
elif სხვა_პირობა:
    # ეს ბლოკი შესრულდება თუ პირველი პირობა მცდარია, მაგრამ ეს მართალია
else:
    # ეს შესრულდება თუ არცერთი პირობა არ შესრულდა
```

მაგალითი:
```python
temperature = 25

if temperature > 30:
    print("ცხელა!")
elif temperature >= 20:
    print("თბილა!")
else:
    print("ცივა!")
```

📤 შედეგი:
```
თბილა!
```

---

## 🔸 ჩაშენებული პირობის ოპერატორები (Nested if)

შეგვიძლია `if` ბლოკი ჩავწეროთ სხვა `if` ბლოკში:

```python
age = 20
citizen = True

if age >= 18:
    if citizen:
        print("შეგიძლია არჩევნებში მონაწილება.")
    else:
        print("მოქალაქეობა საჭიროა.")
else:
    print("არ სრულწლოვანი ხარ.")
```

📤 შედეგი:
```
შეგიძლია არჩევნებში მონაწილება.
```

---

## 🔸 მაგალითები პრაქტიკიდან

### 1️⃣ რიცხვის დადებითი/უარყოფითი შემოწმება
```python
num = -5

if num > 0:
    print("დადებითი რიცხვია")
elif num == 0:
    print("ნულოვანია")
else:
    print("უარყოფითია")
```

---

### 2️⃣ სტუდენტის ქულის შეფასება
```python
score = 82

if score >= 90:
    print("შესანიშნავი")
elif score >= 75:
    print("კარგი")
elif score >= 60:
    print("საშუალო")
else:
    print("დაუკმაყოფილებელი")
```

---

### 3️⃣ even/odd (ლუწი ან კენტი)
```python
number = 7

if number % 2 == 0:
    print("ლუწია")
else:
    print("კენტია")
```

---

### 4️⃣ and / or გამოყენება
```python
age = 25
has_ticket = True

if age >= 18 and has_ticket:
    print("შეგიძლია ფილმზე შესვლა")
else:
    print("შესვლა აკრძალულია")
```

---

### 5️⃣ not ოპერატორი
```python
is_raining = False

if not is_raining:
    print("წავიდეთ გასეირნებად!")
else:
    print("სჯობს სახლში დავრჩეთ.")
```

---

## 🔹 Boolean როგორც შედეგი სხვა ოპერაციებიდან

მათემატიკური შედარებები აბრუნებენ Boolean მნიშვნელობას:
```python
print(10 > 5)    # True
print(10 < 5)    # False
```

შეგვიძლია ეს მნიშვნელობები შევინახოთ ცვლადში:
```python
result = (15 % 2 == 0)
print(result)  # False
```

---

## 🔸 ერთი ხაზით პირობის ჩანაწერი (Conditional Expression / Ternary Operator)

ინახავს შედეგს ერთ ხაზში:
```python
age = 20
status = "სრულწლოვანი" if age >= 18 else "არასრულწლოვანი"
print(status)
```

📤 შედეგი:
```
სრულწლოვანი
```

---

## ✅ შეჯამება

- **Boolean ტიპი** → `True` / `False`  
- **შედარების ოპერატორები** → `==`, `!=`, `>`, `<`, `>=`, `<=`  
- **ლოგიკური ოპერატორები** → `and`, `or`, `not`  
- **პირობის ოპერატორები** → `if`, `elif`, `else`  
- **ჩაშენებული პირობები** → `if` ბლოკები ერთმანეთს შიგნით  
- **ტერნარული ოპერატორი** → ერთ ხაზზე პირობითი გამოთვლა  

---

## 💡 დავალება სტუდენტებისთვის

- დაწერე პროგრამა, რომელიც ამოწმებს, არის თუ არა რიცხვი 3-ის და 5-ის ჯერადი.  
შეამოწმე ტემპერატურის მიხედვით, როგორ უნდა ჩაიცვას ადამიანი:  
>25° → "მოკლემკლავიანი მაისური"  
15–25° → "სვიტერი"  
<15° → "ჟაკეტი"  
- შექმენი პროგრამა, რომელიც ამოწმებს, შეუძლია თუ არა მომხმარებელს ავტობუსში უფასოდ მგზავრობა:  
(პირობა: ასაკი < 7 ან ასაკი > 65)


```py title="Tasks.py" linenums="1"

# პროგრამა 1: რიცხვის შემოწმება, არის თუ არა 3-ის და 5-ის ჯერადი
number = int(input("შეიყვანეთ რიცხვი: "))

if number % 3 == 0 and number % 5 == 0:
    print(f"{number} არის 3-ის და 5-ის ჯერადი.")
else:
    print(f"{number} არ არის 3-ის და 5-ის ჯერადი.")

# პროგრამა 2: ტემპერატურის მიხედვით ჩაცმის რჩევა
temperature = float(input("შეიყვანეთ ტემპერატურა (C°): "))

if temperature > 25:
    print("მოკლემკლავიანი მაისური")
elif 15 <= temperature <= 25:
    print("სვიტერი")
else:
    print("ჟაკეტი")

# პროგრამა 3: უფასო მგზავრობის შემოწმება
age = int(input("შეიყვანეთ თქვენი ასაკი: "))

if age < 7 or age > 65:
    print("თქვენ შეგიძლიათ უფასოდ იმგზავროთ ავტობუსით!")
else:
    print("თქვენ არ შეგიძლიათ უფასოდ მგზავრობა.")

```

---


# 💡 Python – პრაქტიკული დავალებები ლოგიკურ და პირობით ოპერატორებზე

## 🔹 I დონე – მარტივი (თავსატეხის გარეშე)
ეს დავალებები მოიცავს მხოლოდ ერთ ან ორ პირობას.

1️⃣ შეამოწმე, არის თუ არა რიცხვი დადებითი.  
2️⃣ შეამოწმე, არის თუ არა რიცხვი კენტი.  
3️⃣ შეამოწმე, უდრის თუ არა ორი ცვლადის მნიშვნელობა.  
4️⃣ შეამოწმე, არის თუ არა რიცხვი 10-ზე მეტი.  
5️⃣ შეამოწმე, არის თუ არა სტრინგი ცარიელი (`len(text) == 0`).  
6️⃣ შეამოწმე, იწყება თუ არა ტექსტი ასო „A“-თი.  
7️⃣ შეამოწმე, მთავრდება თუ არა ტექსტი სიმბოლოთი `"!"`.  
8️⃣ შეამოწმე, არის თუ არა რიცხვი ნულოვანი.  
9️⃣ შეამოწმე, შედის თუ არა რიცხვი დიაპაზონში 1-დან 100-მდე.  
🔟 შეამოწმე, უდრის თუ არა მომხმარებლის შეყვანილი პაროლი `"python"`-ს.

---

## 🔸 II დონე – საშუალო სირთულის
ამ დავალებებში საჭიროა `and`, `or`, `not` ან ჩაშენებული პირობები.

11️⃣ შეამოწმე, არის თუ არა რიცხვი ერთდროულად 3-ის და 5-ის ჯერადი.  
12️⃣ შეამოწმე, არის თუ არა ტემპერატურა მოსახერხებელი (`20–25` გრადუსი შორის).  
13️⃣ შეამოწმე, შეუძლია თუ არა ადამიანს მანქანის მართვა (`age >= 18`).  
14️⃣ შეამოწმე, შეუძლია თუ არა უფასო მგზავრობა (`age < 7 or age > 65`).  
15️⃣ შეამოწმე, არის თუ არა რიცხვი დადებითი და ლუწი.  
16️⃣ შეამოწმე, არ არის თუ არა სიტყვა `"admin"`.  
17️⃣ შეამოწმე, შეესაბამება თუ არა მომხმარებლის სახელი და პაროლი (ორი ცვლადი).  
18️⃣ შეამოწმე, არის თუ არა წელი ნაკიანი (4-ზე იყოფა და 100-ზე არა, ან 400-ზე იყოფა).  
19️⃣ შეამოწმე, ემთხვევა თუ არა ორი ტექსტი ასო-ასო, რეგისტრის უგულებელყოფით (`.lower()`).  
20️⃣ შეამოწმე, არის თუ არა სტრინგში სიტყვა `"Python"`.

---

## 🔹 III დონე – პირობითი არჩევანი (if / elif / else)

21️⃣ შექმენი პროგრამა, რომელიც ბეჭდავს:  
- `"ცივა"` თუ ტემპერატურა < 15  
- `"თბილა"` თუ 15–25 შორისაა  
- `"ცხელა"` თუ >25  

22️⃣ შეამოწმე, არის თუ არა ქულა საკმარისი გამოცდის ჩასაბარებლად (>= 60).  
23️⃣ ქულების მიხედვით დაბეჭდე შეფასება:  
- >=90: "შესანიშნავი"  
- 75–89: "კარგი"  
- 60–74: "საშუალო"  
- <60: "დაუკმაყოფილებელი"

24️⃣ შეამოწმე მომხმარებლის ასაკის მიხედვით:  
- <13 → "ბავშვი"  
- 13–17 → "მოზარდი"  
- 18–64 → "ზრდასრული"  
- >=65 → "პენსიონერი"

25️⃣ შექმენი პროგრამა, რომელიც ამოწმებს, რამდენი ციფრია რიცხვში (`len(str(num))`).

---

## 🔸 IV დონე – ჩაშენებული პირობები (Nested if)

26️⃣ შეამოწმე, შეუძლია თუ არა სტუდენტს გამოცდის ჩაბარება:  
- ქულა ≥ 60  
- დასწრება ≥ 75%  
თუ ორივე პირობა შესრულდა → `"ჩააბარა"`.

27️⃣ მომხმარებელი წერს მომხმარებლის სახელს და პაროლს.  
თუ მომხმარებლის სახელი `"admin"` და პაროლი `"1234"` → `"Welcome!"`,  
სხვა შემთხვევაში `"Access denied"`.

28️⃣ თუ მომხმარებელი შეყავს ასაკი და მოქალაქეობის სტატუსი (True/False),  
დაბეჭდე შეუძლია თუ არა არჩევნებში მონაწილეობა.

29️⃣ შეამოწმე, არის თუ არა რიცხვი დადებითი და ასევე ლუწი:  
თუ კი → `"დადებითი ლუწი"`,  
თუ დადებითია, მაგრამ კენტი → `"დადებითი კენტი"`,  
თუ უარყოფითია → `"უარყოფითი"`.

30️⃣ მომხმარებლის ასაკის და ბილეთის ქონის მიხედვით დაბეჭდე:  
- აქვს ბილეთი და სრულწლოვანია → `"შეგიძლია შესვლა"`  
- ბილეთი არ აქვს → `"შეიძინე ბილეთი"`  
- არასრულწლოვანია → `"შესვლა აკრძალულია"`

---

## 🔹 V დონე – პრაქტიკული სცენარები

31️⃣ მაღაზიაში ფასდაკლება:  
- თუ თანხა > 100 → 10% ფასდაკლება  
- თუ თანხა > 50 → 5% ფასდაკლება  
- სხვა შემთხვევაში ფასდაკლება არაა.

32️⃣ პროგრამა ამოწმებს პაროლის სიძლიერეს:  
- სიგრძე >= 8  
- შეიცავს `"@"` ან `"!"`  
თუ ორივე პირობა შესრულდა → `"ძლიერი პაროლი"`

33️⃣ მომხმარებელი შეიყვანს საათს (0–23):  
დაბეჭდე დღეის მონაკვეთი:  
- 0–11 → `"დილა"`  
- 12–17 → `"შუადღე"`  
- 18–23 → `"საღამო"`

34️⃣ შექმენი პროგრამა, რომელიც ამოწმებს, შეიძლება თუ არა პირის შესვლა ღონისძიებაზე:  
- ასაკი >= 18  
- ბილეთი აქვს  
- ადგილი თავისუფალია  
თუ ყველა პირობა მართალია → `"შესვლა ნებადართულია"`

35️⃣ შეამოწმე, არის თუ არა სიტყვა პალინდრომი (წინ და უკან ერთნაირად იკითხება).

---

## 🔸 VI დონე – შემოქმედებითი დავალებები

36️⃣ დაწერე პროგრამა, რომელიც მომხმარებლის მიერ შეყვანილი რიცხვის მიხედვით ამბობს:
- `"Fizz"` თუ 3-ზე იყოფა,
- `"Buzz"` თუ 5-ზე იყოფა,
- `"FizzBuzz"` თუ ორივეზე იყოფა,
- სხვა შემთხვევაში დაბეჭდავს თვითონ რიცხვს.

37️⃣ შექმენი პროგრამა, რომელიც ადარებს ორ რიცხვს და ბეჭდავს რომელია დიდი ან ტოლია თუ არა.  
38️⃣ მომხმარებელი წერს ტემპერატურას ცელსიუსში — თუ >30 ბეჭდავს `"ძალიან ცხელა"`,  
თუ 15–30 `"ნორმალური ტემპერატურაა"`,  
თუ <15 `"ცივა"`.

39️⃣ შექმენი პროგრამა, რომელიც ითვლის, არის თუ არა წელი "ნაკიანი" და ბეჭდავს შესაბამის შეტყობინებას.  
40️⃣ მომხმარებელი შეიყვანს ქულას და პროგრამა დაადგენს, შეუძლია თუ არა სტიპენდიაზე განაცხადის გაკეთება (>=85).

---

## 🔹 VII დონე – Boolean ცვლადებით და გამოთვლებით

41️⃣ `is_even = (number % 2 == 0)`  
დაბეჭდე ტექსტი `"ლუწია"` თუ `is_even` მართალია, სხვაგვარად `"კენტია"`.

42️⃣ მომხმარებელი შეიყვანს ორ რიცხვს, შეამოწმე, ორივე დადებითია თუ არა.  
43️⃣ შეამოწმე, არის თუ არა ტექსტში `"python"` ან `"java"`.  
44️⃣ შეამოწმე, არ არის თუ არა ტექსტი ცარიელი (`not text`).  
45️⃣ შეამოწმე, არის თუ არა სტუდენტი გამოცდაზე მისული და ჩაბარებული (ორი Boolean ცვლადი).  
46️⃣ გამოიყენე ტერნარული ოპერატორი:
```python
result = "ლუწია" if num % 2 == 0 else "კენტია"
```
47️⃣ ანალოგიურად, დაწერე ტერნარული მაგალითები ტემპერატურასა და ასაკზე.  
48️⃣ დაწერე პროგრამა, რომელიც ტექსტს აქცევს lowercase-ში და ამოწმებს `"python"` სიტყვას.  
49️⃣ დაწერე პატარა თამაშის მოდელი: თუ მომხმარებელმა გამოიცნო რიცხვი → `"გილოცავ!"`, სხვა შემთხვევაში `"თავიდან სცადე!"`.  
50️⃣ შექმენი Boolean ცვლადი `has_permission = True` და შეამოწმე, შეუძლია თუ არა შესვლა საიტზე.

---

📘 **საკვანძო იდეა**:  
პირობის ოპერატორები გვაძლევს საშუალებას **პროგრამამ „იფიქროს“**, ანუ იმოქმედოს ლოგიკის მიხედვით — ზუსტად ისე, როგორც ჩვენ ვიღებთ გადაწყვეტილებებს ყოველდღიურ ცხოვრებაში.



<!-- 
Python Boolean ლოგიკისა და პირობების დავალებების გადაწყვეტები
ქვემოთ მოცემულია ყველა დავალების გადაწყვეტა Python-ში, ორგანიზებული დონეების მიხედვით. თითოეულ გადაწყვეტას თან ახლავს მოკლე განმარტება.

```py title="Tasks.py" linenums="1"


🔹 I დონე – მარტივი (თავსატეხის გარეშე)

1️⃣ შეამოწმე, არის თუ არა რიცხვი დადებითი.

number = 5
if number > 0:
    print("რიცხვი დადებითია")
else:
    print("რიცხვი არ არის დადებითი")

განმარტება: > ოპერატორი ამოწმებს, აღემატება თუ არა number ნულს.
2️⃣ შეამოწმე, არის თუ არა რიცხვი კენტი.
number = 7
if number % 2 != 0:
    print("რიცხვი კენტია")
else:
    print("რიცხვი ლუწია")

განმარტება: % ოპერატორი ანხორციელებს გაყოფის ნაშთს. თუ ნაშთი 2-ზე გაყოფისას არ უდრის 0-ს, რიცხვი კენტია.
3️⃣ შეამოწმე, უდრის თუ არა ორი ცვლადის მნიშვნელობა.
a = 10
b = 10
if a == b:
    print("ცვლადები ერთმანეთს უდრის")
else:
    print("ცვლადები განსხვავებულია")

განმარტება: == ამოწმებს თანასწორობას.
4️⃣ შეამოწმე, არის თუ არა რიცხვი 10-ზე მეტი.
number = 15
if number > 10:
    print("რიცხვი 10-ზე მეტია")
else:
    print("რიცხვი 10-ზე ნაკლები ან ტოლია")

განმარტება: > ამოწმებს, აღემატება თუ არა number 10-ს.
5️⃣ შეამოწმე, არის თუ არა სტრინგი ცარიელი.
text = ""
if len(text) == 0:
    print("სტრინგი ცარიელია")
else:
    print("სტრინგი არ არის ცარიელი")

განმარტება: len() ითვლის სტრიქონის სიგრძეს. თუ 0-ია, სტრიქონი ცარიელია.
6️⃣ შეამოწმე, იწყება თუ არა ტექსტი ასო „A“-თი.
text = "არტემი"
if text.startswith("A"):
    print("ტექსტი იწყება A-თი")
else:
    print("ტექსტი არ იწყება A-თი")

განმარტება: startswith() ამოწმებს, იწყება თუ არა სტრიქონი მითითებული სიმბოლოთი.
7️⃣ შეამოწმე, მთავრდება თუ არა ტექსტი სიმბოლოთი "!".
text = "გამარჯობა!"
if text.endswith("!"):
    print("ტექსტი მთავრდება !-ით")
else:
    print("ტექსტი არ მთავრდება !-ით")

განმარტება: endswith() ამოწმებს, მთავრდება თუ არა სტრიქონი მითითებული სიმბოლოთი.
8️⃣ შეამოწმე, არის თუ არა რიცხვი ნულოვანი.
number = 0
if number == 0:
    print("რიცხვი ნულის ტოლია")
else:
    print("რიცხვი არ არის ნული")

განმარტება: == ამოწმებს, უდრის თუ არა number ნულს.
9️⃣ შეამოწმე, შედის თუ არა რიცხვი დიაპაზონში 1-დან 100-მდე.
number = 42
if 1 <= number <= 100:
    print("რიცხვი 1-დან 100-მდეა")
else:
    print("რიცხვი 1-დან 100-ის ფარგლებში არაა")

განმარტება: <= ამოწმებს, არის თუ არა number 1-ის ჩათვლით და 100-ის ჩათვლით.
🔟 შეამოწმე, უდრის თუ არა მომხმარებლის შეყვანილი პაროლი "python"-ს.
password = input("შეიყვანე პაროლი: ")
if password == "python":
    print("პაროლი სწორია")
else:
    print("პაროლი არასწორია")

განმარტება: input() იღებს მომხმარებლის შეყვანას, == ამოწმებს თანასწორობას.

🔸 II დონე – საშუალო სირთულის
11️⃣ შეამოწმე, არის თუ არა რიცხვი ერთდროულად 3-ის და 5-ის ჯერადი.
number = 15
if number % 3 == 0 and number % 5 == 0:
    print("რიცხვი 3-ის და 5-ის ჯერადია")
else:
    print("რიცხვი არ არის 3-ის ან/და 5-ის ჯერადი")

განმარტება: and აერთიანებს ორ პირობას: number უნდა იყოფოდეს 3-ზე და 5-ზე ნაშთის გარეშე.
12️⃣ შეამოწმე, არის თუ არა ტემპერატურა მოსახერხებელი (20–25 გრადუსი).
temperature = 22
if 20 <= temperature <= 25:
    print("ტემპერატურა მოსახერხებელია")
else:
    print("ტემპერატურა არ არის მოსახერხებელი")

განმარტება: <= ამოწმებს, არის თუ არა temperature 20-დან 25-მდე.
13️⃣ შეამოწმე, შეუძლია თუ არა ადამიანს მანქანის მართვა (age >= 18).
age = 20
if age >= 18:
    print("შეგიძლია მანქანის მართვა")
else:
    print("არ შეგიძლია მანქანის მართვა")

განმარტება: >= ამოწმებს, არის თუ არა age 18-ის ტოლი ან მეტი.
14️⃣ შეამოწმე, შეუძლია თუ არა უფასო მგზავრობა (age < 7 or age > 65).
age = 70
if age < 7 or age > 65:
    print("შეგიძლია უფასო მგზავრობა")
else:
    print("უფასო მგზავრობა არ შეგიძლია")

განმარტება: or ამოწმებს, თუ ერთ-ერთი პირობა მაინც ჭეშმარიტია.
15️⃣ შეამოწმე, არის თუ არა რიცხვი დადებითი და ლუწი.
number = 4
if number > 0 and number % 2 == 0:
    print("რიცხვი დადებითი და ლუწია")
else:
    print("რიცხვი არ არის დადებითი ან/და ლუწი")

განმარტება: and აერთიანებს ორ პირობას: number უნდა იყოს დადებითი და ლუწი.
16️⃣ შეამოწმე, არ არის თუ არა სიტყვა "admin".
word = "user"
if word != "admin":
    print("სიტყვა არ არის admin")
else:
    print("სიტყვა admin-ია")

განმარტება: != ამოწმებს, არის თუ არა word განსხვავებული "admin"-ისგან.
17️⃣ შეამოწმე, შეესაბამება თუ არა მომხმარებლის სახელი და პაროლი.
username = "giorgi"
password = "1234"
if username == "giorgi" and password == "1234":
    print("სახელი და პაროლი სწორია")
else:
    print("სახელი ან პაროლი არასწორია")

განმარტება: and ამოწმებს, ორივე პირობა ჭეშმარიტია თუ არა.
18️⃣ შეამოწმე, არის თუ არა წელი ნაკიანი.
year = 2024
if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:
    print("წელი ნაკიანია")
else:
    print("წელი არ არის ნაკიანი")

განმარტება: ნაკიანი წელი იყოფა 4-ზე, მაგრამ არა 100-ზე, ან იყოფა 400-ზე.
19️⃣ შეამოწმე, ემთხვევა თუ არა ორი ტექსტი ასო-ასო, რეგისტრის უგულებელყოფით.
text1 = "Hello"
text2 = "hello"
if text1.lower() == text2.lower():
    print("ტექსტები ემთხვევა")
else:
    print("ტექსტები არ ემთხვევა")

განმარტება: .lower() გარდაქმნის ტექსტს მცირე ასოებად, == ამოწმებს თანასწორობას.
20️⃣ შეამოწმე, არის თუ არა სტრინგში სიტყვა "Python".
text = "I love Python programming"
if "Python" in text:
    print("სტრინგი შეიცავს Python-ს")
else:
    print("სტრინგი არ შეიცავს Python-ს")

განმარტება: in ამოწმებს, არის თუ არა "Python" სტრიქონის ნაწილი.

🔹 III დონე – პირობითი არჩევანი (if / elif / else)
21️⃣ ტემპერატურის შემოწმება.
temperature = 20
if temperature < 15:
    print("ცივა")
elif 15 <= temperature <= 25:
    print("თბილა")
else:
    print("ცხელა")

განმარტება: elif ამოწმებს დიაპაზონს, else იჭერს ყველა სხვა შემთხვევას.
22️⃣ შეამოწმე, არის თუ არა ქულა საკმარისი გამოცდის ჩასაბარებლად.
score = 65
if score >= 60:
    print("გამოცდა ჩაბარებულია")
else:
    print("გამოცდა არ ჩაბარებულა")

განმარტება: >= ამოწმებს, აღემატება თუ არა score 60-ს.
23️⃣ ქულების მიხედვით შეფასება.
score = 82
if score >= 90:
    print("შესანიშნავი")
elif 75 <= score <= 89:
    print("კარგი")
elif 60 <= score <= 74:
    print("საშუალო")
else:
    print("დაუკმაყოფილებელი")

განმარტება: elif ამოწმებს თანმიმდევრულად სხვადასხვა დიაპაზონს.
24️⃣ მომხმარებლის ასაკის კატეგორია.
age = 16
if age < 13:
    print("ბავშვი")
elif 13 <= age <= 17:
    print("მოზარდი")
elif 18 <= age <= 64:
    print("ზრდასრული")
else:
    print("პენსიონერი")

განმარტება: elif ამოწმებს ასაკის დიაპაზონებს თანმიმდევრობით.
25️⃣ რიცხვის ციფრების რაოდენობა.
number = 1234
digit_count = len(str(number))
print(f"რიცხვში {digit_count} ციფრია")

განმარტება: str() გარდაქმნის რიცხვს სტრიქონად, len() ითვლის სიგრძეს.

🔸 IV დონე – ჩაშენებული პირობები (Nested if)
26️⃣ სტუდენტის გამოცდის ჩაბარება.
score = 70
attendance = 80
if score >= 60:
    if attendance >= 75:
        print("ჩააბარა")
    else:
        print("დასწრება არასაკმარისია")
else:
    print("ქულა არასაკმარისია")

განმარტება: if ჩაშენებულია სხვა if-ში, ამოწმებს ორივე პირობას.
27️⃣ მომხმარებლის სახელი და პაროლი.
username = "admin"
password = "1234"
if username == "admin":
    if password == "1234":
        print("Welcome!")
    else:
        print("Access denied")
else:
    print("Access denied")

განმარტება: ჩაშენებული if ამოწმებს პაროლს მხოლოდ მაშინ, თუ სახელი სწორია.
28️⃣ არჩევნებში მონაწილეობა.
age = 20
is_citizen = True
if age >= 18:
    if is_citizen:
        print("შეგიძლია არჩევნებში მონაწილეობა")
    else:
        print("მოქალაქეობა საჭიროა")
else:
    print("ასაკი არასაკმარისია")

განმარტება: ორი პირობა ამოწმებს ასაკს და მოქალაქეობას.
29️⃣ დადებითი და ლუწი/კენტი.
number = 6
if number > 0:
    if number % 2 == 0:
        print("დადებითი ლუწი")
    else:
        print("დადებითი კენტი")
else:
    print("უარყოფითი")

განმარტება: ჩაშენებული if ამოწმებს ლუწობას, თუ რიცხვი დადებითია.
30️⃣ ასაკი და ბილეთი.
age = 20
has_ticket = True
if has_ticket:
    if age >= 18:
        print("შეგიძლია შესვლა")
    else:
        print("შესვლა აკრძალულია")
else:
    print("შეიძინე ბილეთი")

განმარტება: ჯერ ამოწმებს ბილეთის არსებობას, შემდეგ ასაკს.

🔹 V დონე – პრაქტიკული სცენარები
31️⃣ მაღაზიის ფასდაკლება.
amount = 120
if amount > 100:
    discount = amount * 0.1
    print(f"ფასი ფასდაკლებით: {amount - discount}")
elif amount > 50:
    discount = amount * 0.05
    print(f"ფასი ფასდაკლებით: {amount - discount}")
else:
    print(f"ფასი: {amount}")

განმარტება: elif ამოწმებს სხვადასხვა თანხის დიაპაზონს.
32️⃣ პაროლის სიძლიერე.
password = "secret@123"
if len(password) >= 8 and ("@" in password or "!" in password):
    print("ძლიერი პაროლი")
else:
    print("სუსტი პაროლი")

განმარტება: and და or ამოწმებენ სიგრძეს და სიმბოლოებს.
33️⃣ დღის მონაკვეთი.
hour = 14
if 0 <= hour <= 11:
    print("დილა")
elif 12 <= hour <= 17:
    print("შუადღე")
elif 18 <= hour <= 23:
    print("საღამო")
else:
    print("არასწორი საათი")

განმარტება: elif ყოფს დღეს საათების მიხედვით.
34️⃣ ღონისძიებაზე შესვლა.
age = 20
has_ticket = True
has_seat = True
if age >= 18 and has_ticket and has_seat:
    print("შესვლა ნებადართულია")
else:
    print("შესვლა აკრძალულია")

განმარტება: and ამოწმებს სამივე პირობას.
35️⃣ პალინდრომის შემოწმება.
word = "radar"
if word == word[::-1]:
    print("სიტყვა პალინდრომია")
else:
    print("სიტყვა არ არის პალინდრომი")

განმარტება: [::-1] აბრუნებს სტრიქონს, == ამოწმებს თანასწორობას.

🔸 VI დონე – შემოქმედებითი დავალებები
36️⃣ FizzBuzz.
number = 15
if number % 3 == 0 and number % 5 == 0:
    print("FizzBuzz")
elif number % 3 == 0:
    print("Fizz")
elif number % 5 == 0:
    print("Buzz")
else:
    print(number)

განმარტება: თანმიმდევრულად ამოწმებს ჯერადობას 3-ზე და 5-ზე.
37️⃣ ორი რიცხვის შედარება.
a = 10
b = 20
if a > b:
    print(f"{a} მეტია {b}-ზე")
elif a < b:
    print(f"{b} მეტია {a}-ზე")
else:
    print("რიცხვები ტოლია")

განმარტება: elif ამოწმებს სამივე შესაძლო შემთხვევას.
38️⃣ ტემპერატურის შემოწმება.
temperature = 32
if temperature > 30:
    print("ძალიან ცხელა")
elif 15 <= temperature <= 30:
    print("ნორმალური ტემპერატურაა")
else:
    print("ცივა")

განმარტება: elif ყოფს ტემპერატურას სამ კატეგორიად.
39️⃣ ნაკიანი წელი.
year = 2024
if (year % 4 == 0 and year % 100 != 0) or year % 400 == 0:
    print("წელი ნაკიანია")
else:
    print("წელი არ არის ნაკიანი")

განმარტება: იგივე ლოგიკა, რაც #18-ში.
40️⃣ სტიპენდიის განაცხადი.
score = 90
if score >= 85:
    print("შეგიძლია სტიპენდიაზე განაცხადის გაკეთება")
else:
    print("ქულა არასაკმარისია")

განმარტება: >= ამოწმებს, აღემატება თუ არა score 85-ს.

🔹 VII დონე – Boolean ცვლადებით და გამოთვლებით
41️⃣ ლუწი/კენტი Boolean-ით.
number = 4
is_even = (number % 2 == 0)
if is_even:
    print("ლუწია")
else:
    print("კენტია")

განმარტება: is_even ინახავს Boolean-ს, if ამოწმებს მის მნიშვნელობას.
42️⃣ ორივე რიცხვი დადებითია.
num1 = 5
num2 = 3
if num1 > 0 and num2 > 0:
    print("ორივე რიცხვი დადებითია")
else:
    print("ერთი ან ორივე რიცხვი არ არის დადებითი")

განმარტება: and ამოწმებს ორივე პირობას.
43️⃣ ტექსტში "python" ან "java".
text = "I know python"
if "python" in text or "java" in text:
    print("ტექსტი შეიცავს python ან java")
else:
    print("ტექსტი არ შეიცავს python ან java")

განმარტება: or ამოწმებს, არის თუ არა ერთ-ერთი სიტყვა.
44️⃣ ტექსტი ცარიელია.
text = ""
if not text:
    print("ტექსტი ცარიელია")
else:
    print("ტექსტი არ არის ცარიელი")

განმარტება: not text ამოწმებს, არის თუ არა სტრიქონი ცარიელი.
45️⃣ სტუდენტის გამოცდის სტატუსი.
is_present = True
has_passed = True
if is_present and has_passed:
    print("სტუდენტმა ჩააბარა გამოცდა")
else:
    print("სტუდენტმა ვერ ჩააბარა ან არ იყო გამოცდაზე")

განმარტება: and ამოწმებს ორივე Boolean ცვლადს.
46️⃣ ტერნარული ოპერატორი ლუწი/კენტზე.
num = 6
result = "ლუწია" if num % 2 == 0 else "კენტია"
print(result)

განმარტება: ტერნარული ოპერატორი ამოწმებს ლუწობას ერთ ხაზში.
47️⃣ ტერნარული მაგალითები.
# ტემპერატურა
temperature = 22
temp_result = "მოსახერხებელია" if 20 <= temperature <= 25 else "არასახერხებელია"
print(temp_result)

# ასაკი
age = 20
age_result = "სრულწლოვანი" if age >= 18 else "არასრულწლოვანი"
print(age_result)

განმარტება: ტერნარული ოპერატორი ამარტივებს if-else-ს.
48️⃣ ტექსტის lowercase და python-ის შემოწმება.
text = "I LOVE PYTHON"
if "python" in text.lower():
    print("ტექსტი შეიცავს python-ს")
else:
    print("ტექსტი არ შეიცავს python-ს")

განმარტება: .lower() გარდაქმნის ტექსტს, in ამოწმებს სიტყვას.
49️⃣ რიცხვის გამოცნობის თამაში.
secret = 7
guess = 7
if guess == secret:
    print("გილოცავ!")
else:
    print("თავიდან სცადე!")

განმარტება: == ამოწმებს, ემთხვევა თუ არა გამოცნობა.
50️⃣ საიტზე შესვლა.
has_permission = True
if has_permission:
    print("საიტზე შესვლა ნებადართულია")
else:
    print("საიტზე შესვლა აკრძალულია")

``` -->

განმარტება: Boolean ცვლადი has_permission განსაზღვრავს შესვლას.

დამატებითი შენიშვნა
თითოეული მაგალითი შეიძლება გამოცდოთ სხვადასხვა მნიშვნელობებით, რათა სტუდენტებმა სხვადასხვა შედეგი ნახონ. input()-ის გამოყენება შეიძლება დაემატოს, თუ მომხმარებლის ინტერაქციაა საჭირო.